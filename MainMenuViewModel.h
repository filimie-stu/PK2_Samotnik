#pragma once

/**
 * \brief Model widoku MainMenuView. 
 * 
 * Jest to prosta struktura zawierająca zewnętrzne informacje potrzebne do poprawnego wyświetlenia widoku.
*/
typedef struct main_menu_view_model
{
    int displayContinueButton;      //!< flaga informująca o tym czy wyświetlony powinien zostać przycisk kontynuacji gry.

} MainMenuViewModel;